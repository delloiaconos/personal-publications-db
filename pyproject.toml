[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "personal-publication-db"
version = "0.1.0"
description = "Personal publication database utility"
requires-python = ">=3.10"
dependencies = [
    "click",
    "Jinja2",
    "requests",
]

[project.scripts]
ppubdb = "ppubdb:ppdb"

[tool.setuptools]
py-modules = ["ppubdb"]
packages = ["ppubdb_resources"]

[tool.setuptools.package-data]
ppubdb_resources = ["db.sql", "templates/*.j2"]
